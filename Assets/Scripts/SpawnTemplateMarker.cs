using UnityEngine;

/// <summary>Marks inactive spawn prefab roots so <see cref="SandboxEditorToolbarFrame"/> clear-scene does not destroy them.</summary>
public sealed class SpawnTemplateMarker : MonoBehaviour
{
}
